<!DOCTYPE html>
<html lang="es">

  <head>

    <meta charset="UTF-8">    
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <title><% title ? `${title} | ` : '' %>FUNKOSHOP</title>

    <!-- Estilos Globales -->
    <link rel="stylesheet" href="./css/index.css">

    <% if( styles ){ %>

      <!-- Estilos Internos -->
      <% styles.forEach( style => { %>

        <link rel="stylesheet" href="<%=`./css/${style}.css`%>">

      <% }); %>
    <% } %>
    
  </head>

  <body>

    <%-include("./header")%>

    